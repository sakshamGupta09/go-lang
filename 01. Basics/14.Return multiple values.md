# Returning multiple values

- Sometimes, we might need to return multiple values from a function. Go provides a way to handle that easily.
- Otherwise we would have to use composite types like maps, objects or arrays to return multiple values and later destructure them.
- It also provides a convinient way of managing errors.
