# Variadic params and functions

- Functions that can take variable number of parameters (o to n). When we call these functions we can provide 0 to n parameters.
- The variable list of parameters is consolidated in a variable that we can iterate on.
