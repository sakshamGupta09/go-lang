# Maps

- To store key value pairs like in a dictionary.
- Insertion, deletion and search by key are constant time operations.
- Maps are unordered which means when iterating over them, order is not guaranteed.
