# Embed directive

- How static files/ folders are packaged and shipped.
- To include static files and direcories(HTML templates, SQL Migration files, assets) inside your compiled binary so your program can run without depending on external files at runtime.


## Syntax

```go
import (
	_ "embed"
	"fmt"
)

//go:embed file.txt
var content string
```
