
# Command Line Arguments

- To pass some parameters or flags to our program when executed from a terminal or command line.
- To access those passed values, we use command line arguments.
- We can read the command line arguments using `os.Args` method of `os` package.
