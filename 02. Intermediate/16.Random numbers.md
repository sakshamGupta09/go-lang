# Random numbers

- There are some applications where random numbers are useful like Ludo or dice games.


## rand.Intn()

- It generates a pseudo random positive integer between 0 and num-1.


```go
func main() {
	fmt.Println(rand.Intn(10))
}
```
