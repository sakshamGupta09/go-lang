# Strings

- String is a sequence of characters. Each character is a unsigned int8 value.
- Strings are immutable. Once created they cannot be modified. We have to create a new string if we need to modify a string.
