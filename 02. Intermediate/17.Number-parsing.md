# Number Parsing

- To convert a valid string to a number.
- If the conversion is not possible, it throws an error.


## strconv.Atoi(string)

``` go
func main() {
	str := "12345"
	num, err := strconv.Atoi(str)

	if err != nil {
		fmt.Println("CONVERSION FAILED", err)
		return
	}

	fmt.Println("CONVERSION SUCCESSFUL", num)
}
```
