# Http Client

- To make HTTP requests and read response.

```go
client := &http.Client{}
resp, err := client.Get("https://dummyjson.com/products/1")
```

- It returns an error only if URL is invalid or network connectivity. Not 200 status codes do not fall into error category.
