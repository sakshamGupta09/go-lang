# Timers

- This code should execute after certain amount of time not immediately.

## time.Sleep

- Blocks the goroutine completely.
- Can’t cancel or control it.
- Timers are non-blocking + channel-based

##  Basic Timer Example

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	timer := time.NewTimer(2 * time.Second)

	fmt.Println("Waiting...")

	<-timer.C  // waits for timer to fire

	fmt.Println("Timer fired!")
}
```

- `time.NewTimer()` creates a timer which will send a value into channel after 2 seconds.
- `timer.C` is a channel
- After 2 seconds → it sends a value into the channel
- `<-timer.C` waits for that signal.

## Timeout Pattern

```go
select {
case msg := <-dataChan:
	fmt.Println("Received:", msg)

case <-time.After(2 * time.Second):
	fmt.Println("Timeout! No data received")
}
```

## Resetting a Timer

- Reuse timer instead of creating a new one

```go
timer := time.NewTimer(2 * time.Second)

<-timer.C
fmt.Println("First done")

timer.Reset(2 * time.Second)

<-timer.C
fmt.Println("Second done")
```

## Stopping a Timer

```go
timer := time.NewTimer(2 * time.Second)

stopped := timer.Stop()

if stopped {
	fmt.Println("Timer stopped before firing")
}
```
