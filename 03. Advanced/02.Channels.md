# Channels

- When we use goroutines, they run independently.
- We don't know in which order they will execute and which finishes first.
- Now consider, one goroutine produces some data and another goroutine needs that data. How do they communicate with each other and synchronize.
