# Worker Pools

- A pattern to control concurrency.
- Instead of launching unlimited goroutines to execute unlimited jobs, we create a fixed number of workers that compete with each other to get a job. If all workers are busy, jobs will be queued. Once a worker is free it will pick a job from the queue.
