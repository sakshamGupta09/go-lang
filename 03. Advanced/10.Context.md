# Context

## Create a context

- Use `Background()` when you know it's the root context. Clear intent, it's my root.
- Use `TODO()` when you're unsure and will fix it later. A context should be here but I'll decide later.


```go
```
