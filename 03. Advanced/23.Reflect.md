# Reflect

- For every variable we create, we need to define it's type.

```go
var x int = 10
fmt.Println(x + 5)
```

- Compiler knows x is of type int. What all operations can be done with x. What fields and methods exists on it.
- But sometimes you don’t know the type beforehand.
