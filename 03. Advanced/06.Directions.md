# Directions

- In Go, a normal channel is bidirectional. We can send data to channel and read data from a channel.
- But we can restrict it.

```go
chan<- int   // send-only channel
<-chan int   // receive-only channel
```
