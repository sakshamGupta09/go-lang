# Directions

- Goroutines use channels to send data and read data.
- Pattern is that sender goroutine will send message to a channel and receiver goroutine will read that message from the channel.
- So, we need a way to distinguish between sender channel and receiver channel. A channel to which we can only send values and a channel from which we can only read values.
- It gives us type safety and compile time errors so we do not accidently perform the wrong operation. Also imporves readability.

```go
chan<- int   // send-only channel
<-chan int   // receive-only channel
```

## Prevents bugs at compile time

- Without direction, if a function receives a channel as a parameter, It's not clear if I should use that channel to send data or receive.
- With directions, we get compile time errors. This ensures that sender does not accidently receives and receiver does not accidently sends.

```go
func process(ch chan int) {
	ch <- 10      // maybe unintended
	value := <-ch // maybe unintended
}
```

```go
func sender(ch chan<- int) {
	ch <- 10   // ✅ allowed
	// <-ch ❌ compile error
}

func receiver(ch <-chan int) {
	value := <-ch // ✅ allowed
	// ch <- 10 ❌ compile error
}
```

## Makes code self-documenting

```go
func fetchData(ch chan<- int)
```
- This function only sends data.

## You can convert bidirectional → directional

```go
func send(ch chan<- int) {}
func receive(ch <-chan int) {}

func main() {
	ch := make(chan int)

	send(ch)    // allowed
	receive(ch) // allowed
}
```

## Works perfectly with range

- range only needs receive capability.

```go
func consumer(ch <-chan int) {
	for val := range ch {
		fmt.Println(val)
	}
}
```

## Closing channels

- Only the sender should close the channel

```go
func producer(ch chan<- int) {
	ch <- 1
	close(ch) // ✅ correct
}
```

## Examples

```go
func producer(ch chan<- int) {
	for i := 1; i <= 3; i++ {
		ch <- i
	}
	close(ch)
}

func consumer(ch <-chan int) {
	for val := range ch {
		fmt.Println("Received:", val)
	}
}

func main() {
	ch := make(chan int)

	go producer(ch)
	consumer(ch)
}
```
