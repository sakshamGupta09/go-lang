# Directions

- In Go, a normal channel is bidirectional. We can send data to channel and read data from a channel.
- But we can restrict it.

```go
chan<- int   // send-only channel
<-chan int   // receive-only channel
```

## Prevents bugs at compile time

- Without direction, if a function receives a channel as a parameter, It's not clear if I should use that channel tro send data or receive,

```go
func process(ch chan int) {
	ch <- 10      // maybe unintended
	value := <-ch // maybe unintended
}
```
