# Closing channels

- We close channels to indicate completion. That no more data will be send now.
- So, goroutines thta are waiting for this data can stop waiting.
- We cannot send values after a channel is closed. But we can receive if it's a buffered channel as buffered channel might have some stored values.
